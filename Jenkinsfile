pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                // NOOP
            }
        }
        stage('Test') { 
            steps {
                // NOOP
            }
        }
        stage('Deploy') { 
            steps {
                // NOOP
            }
        }
    }
}